export interface Pet {
  id: number;       // matches Long id in backend
  name: string;     // matches String name in backend
  imageUrl: string; // matches String imageUrl in backend
  quote: string;    // matches String quote in backend
  unlocked: boolean; // matches boolean unlocked in backend
}
