export type Profile = {
  deviceId: string;
  pomodoroMinutes: number;
  shortBreakMinutes: number;
  longBreakMinutes: number;
  goalPerWeek: number;
  theme: string;
  darkMode: boolean;
  shortTermGoal: string;
  longTermGoal: string;
  avatarUrl: string;
};
